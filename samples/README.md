# Samples

This directory stores tracked sample protocol outputs for Godot preview scenes and regression checks.

Generate the basic one-turn battle output with:

```sh
npm run sample:turn
```

Generate the five-member side-view UI preview output with:

```sh
npm run sample:romancing-preview
```
